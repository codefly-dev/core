// Package standards defines the small set of API-type constants used
// across the codefly stack: TCP, HTTP, REST, GRPC.
//
// These constants flow through endpoint definitions and network
// instances; agents and the gateway use them to pick the right transport
// and code-generation pipeline for each declared endpoint.
package standards
