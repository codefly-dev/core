# Bumping protobuf-es in the proto companion

The proto companion bakes one `protoc-gen-es`. Which version it bakes is not a
local choice: the generator writes a `// @generated by protoc-gen-es v<version>`
header and codegen shaped for a specific runtime surface, so a companion whose
es diverges from the `@bufbuild/protobuf` a consumer runs against makes that
consumer's committed `*_pb.ts` drift on every regeneration, forever. That is not
hypothetical — it is [#83](https://github.com/codefly-dev/core/issues/83), where
companion `0.0.11` baked es 2.12.0 against an ecosystem pinned to 2.11.0 and
`sync-drift` reported 35 drifted files on a no-op change.

## Current decision: stay on 2.11.0

Asked and answered in [#482](https://github.com/codefly-dev/core/issues/482):
the companion stays at **2.11.0**. The reasoning, so the next person does not
re-derive it:

- **The pin is set by consumers, not by this repository.** What matters is the
  version a consumer's **committed** bindings were generated against.
  `codefly-dev/module-saas-starter` commits 103 `*_pb.ts`, pins
  `@bufbuild/protoc-gen-es` at exactly `2.11.0`, and its committed headers read
  `// @generated by protoc-gen-es v2.11.0`. Core can bake whatever it likes; if
  the companion disagrees, all 103 drift — which is #83 verbatim. Moving the
  companion first inverts the dependency.

  Be careful which consumers count as evidence. `codefly-dev/cli-frontend`
  declares `^2.11.0`, but that is a caret range that today resolves to 2.15.0,
  it gitignores `/src/gen`, and it generates from its own
  `./node_modules/.bin/protoc-gen-es` rather than the companion's. It has no
  committed bindings to drift and constrains nothing. Only repositories that
  commit generated output are the constraint.
- **Nothing needs a newer es.** No open work depends on anything released after
  2.11.0. The facade plugin uses `runNodeJs`, `createEcmaScriptPlugin`,
  `g.import` and `g.importSchema`, all present and unchanged at 2.11.0.
- **The specific version asked about is already stale.** 2.14.1 was superseded
  by 2.15.0. Chasing a named version is how the pin ends up mid-train; when the
  ecosystem does move, it moves to whatever is current then.
- **2.14.1 is not a neutral bump.** It scopes `import_extension` to relative
  import paths only — exactly the import-emission behaviour
  [#439](https://github.com/codefly-dev/core/pull/439) and
  [#443](https://github.com/codefly-dev/core/pull/443) fixed and that
  `grpc_test.go` and `image_test.go` assert on. This is not a dormant option:
  `module-saas-starter` generates with `import_extension=none` and
  `cli-frontend` with `import_extension=js`, so the change lands on the paths
  consumers actually use. Adopting it means re-checking those guards, not just
  moving numbers.

Dependabot will keep proposing the single-package bump on its own, because it
can see `facades/ts/package.json` and none of the other pins — that is exactly
how #447 reddened `main`. An `ignore` for `@bufbuild/*` in
`.github/dependabot.yml` is what stops it; as of this writing that ignore is
**proposed in [#484](https://github.com/codefly-dev/core/pull/484) and not yet
on `main`**, so the bump will keep being re-proposed until it lands. Note the
cost when it does: a bare `ignore` suppresses **security** updates for those
packages too, so a protobuf-es advisory has to be picked up by hand through the
procedure below.

## What holds the pin

Six places, and they must agree:

| location | what it pins | guarded by a test? |
|---|---|---|
| `companions/proto/facades/ts/package.json` | `@bufbuild/protoplugin` | yes |
| `companions/proto/facades/ts/package-lock.json` | the resolved `protoplugin` + `protobuf` closure | yes |
| `companions/proto/Dockerfile:98` | `@bufbuild/protoc-gen-es@` installed via npm | yes |
| `companions/proto/flake.nix:51` | `protocGenEsVersion`, built from source | yes |
| `companions/proto/flake.nix:59` | `hash` — the protobuf-es source tarball | **no** |
| `companions/proto/flake.nix:61` | `npmDepsHash` — its npm dependency closure | **no** |

`protocGenEsRuntimeVersion` in `companions/proto/companion_plugins_test.go`
holds the first four to one version, and this runbook's decision heading is
checked against that same constant — so those five cannot silently diverge. The
lockfile is guarded specifically because `npm ci` installs from it and ignores
what `package.json` asks for; a disagreement between the two would otherwise
surface only once the image was built.

The two Nix hashes stay unguarded, and cannot usefully be checked statically —
nothing can confirm a content hash without fetching the content. They are
self-announcing in exchange: a stale one fails the Nix build loudly rather than
shipping something wrong.

The line numbers above are verified as well. `TestRunbookCitationsPointAtWhatTheyName`
checks that each cited line still contains what this table says it does, so an
edit that shifts one fails there rather than quietly misdirecting the next
reader.

## The procedure

This is one deliberate pull request, not a find-and-replace.

1. **Confirm the ecosystem moved first.** Check what consumers pin for
   `@bufbuild/protobuf` and what their committed `*_pb.ts` headers say. If they
   are still on the old version, stop — bumping core here is what causes drift,
   not what fixes it.
2. Set the three readable version pins (`facades/ts/package.json`, the
   `Dockerfile` npm line, `flake.nix`'s `protocGenEsVersion`) plus
   `protocGenEsRuntimeVersion`. Then regenerate the lockfile with `npm install`
   in `companions/proto/facades/ts` — never hand-edit it. The guard above
   catches a lockfile whose *version* disagrees, but it cannot check an
   integrity hash, and a hand-written one is wrong in a way only `npm ci`
   discovers.
3. **Recompute the two Nix hashes.** Both change with the revision, and a stale
   one fails the build rather than silently passing. Requires `nix`:

   ```sh
   # From companions/proto/. Set each hash to "" (or pkgs.lib.fakeHash — the
   # flake binds pkgs, not a bare lib), build, and take the correct value from
   # the mismatch error.
   nix build .#dockerImage
   ```

   On macOS this needs nix-darwin's `linux-builder` or a Linux VM, the same
   constraint `publish-companions.md` documents — a pure darwin build cannot
   produce the `linux/*` image.

4. Rebuild the image both ways and confirm they ship the same toolchain —
   `codefly companion build proto` for the Dockerfile path, `nix build
   .#dockerImage` for the flake path. The README explains why both exist.
5. **Regenerate TypeScript bindings and read the diff.** The `@generated by`
   header changes at minimum. Anything structural — especially import paths,
   given the `import_extension` and `map_imports` changes on the 2.14/2.15
   train — means `grpc_test.go` and `image_test.go` need re-reading, not just
   re-running.
6. Bump `companions/proto/info.codefly.yaml` and publish per
   [`publish-companions.md`](publish-companions.md). Merging here publishes
   nothing.

## What would change the decision

A consumer moving its runtime, or a protobuf-es security advisory. Either way
the answer is the procedure above, aimed at whatever version is current — not
at 2.14.1.
