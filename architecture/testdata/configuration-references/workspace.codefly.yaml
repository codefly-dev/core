name: codefly-config-refs
layout: flat
# worker reaches host only through a workspace configuration group the
# composition root writes (`platform`); gateway depends on worker and declares a
# group referencing it back, which must not close a cycle.
services:
    - name: host
    - name: worker
    - name: gateway
